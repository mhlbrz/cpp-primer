// assuming i,j,k ints
// explain i != j < k
//
// i != j evaluates as bool, so it returns 1 or 0
// then it passed to evaluation (1 or 0) < k
// if k is an integer that not within range of 0 and 1
// this makes no sence
