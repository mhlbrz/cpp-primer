/*
 * List some drawbacks of using an array instead of a vector:
 * - Cannot be dynamicly changed, has a fixed size, and size must be known
 * exactly
 * - Size must be calculated, C++17 has std::size()
 * - References and pointers can be obscured
 */
